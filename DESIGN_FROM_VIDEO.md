# 从官方演示视频逐帧反推的闪念胶囊设计

来源：Smartisan OS 3.7「闪念胶囊 Idea Pills」官方演示（B 站 BV1zg411w7oL），逐帧抽取分析。
此文件记录**原版真实交互/界面**，作为本项目对齐目标。

## 1. 捕获（录音为主）
- 触发：长按 Home / 耳机线控中键 / 屏幕右边缘左划。
- 按住即开始录音，屏幕**底部弹出一个胶囊气泡**（带朝下小尾巴指向 Home），
  内含**实时增长的音频波形**；松手即保存。
- **保存的是音频本体**；文字是事后从音频转写出来的（存音频 → 转写）。

## 2. 胶囊的两种态
- **收起态**：屏幕右缘一个半透明小把手；生成的胶囊按时间/分类堆在右缘，
  可悬浮在任意 App 之上。向左拖把手 → 展开胶囊列表。
- **展开态（单条，见 t152）**，自上而下：
  1. **顶部一排 5 个分类色标圆钮**：📋便签(蓝) / ❗重要事项(红) / ✅待办事项(橙) /
     💬待发送信息(绿) / 💡灵感(紫)。点一个 = 给胶囊归类并染色。
  2. **胶囊主体**：转写文字（如"锤子"）+ **音频波形 + ▶ 播放按钮**（可回放原声）。
  3. **底部操作栏**（一排图标）：🗑删除 / ✨大爆炸(分词) / 💬发微信 / 📅转日历待办 /
     ↗分享 / ≡转便签(更多)。
  4. **下方自动"网络搜索结果"面板**：拿转写文字自动搜（百度百科/搜狗/有道词典，
     底部可切引擎）+ 本地(联系人/应用/音乐)。

## 3. 分类 = 颜色（贯穿始终）
- 便签=蓝、重要=红、待办=橙、待发送=绿、灵感=紫。
- 列表里胶囊按分类染色堆叠；待办胶囊左侧带**复选框**可勾选完成。

## 4. 胶囊的"去向"（处理）
- 转**便签**（写进便签 App）、转**待办/日历事件**（起始/终止时间、提醒、重复、地点）、
  **发送**给联系人/微信/其它 App、**分享**、**搜索**。
- 处理完进「已完成 / 已处理」；删除进「回收站」，保留 30 天后永久删除。

## 5. 设置页（闪念胶囊测试版）
- 分类：重要事项 / 待办事项 / 待发送信息 / 灵感。
- 开关：本地搜索结果 / 网络搜索结果。
- 入口：已完成 / 已处理 / 回收站。

---

## 对照本项目现状 & 待补
| 原版特性 | 本项目现状 | 计划 |
|---|---|---|
| 录音 + 实时波形 + 松手存 | ❌ 仅实时 STT 出字，无音频 | v0.6：AudioRecord 录音 + 实时波形 |
| 胶囊存音频 + 波形 + ▶播放 | ❌ | v0.6 |
| 从音频转写（Whisper） | ❌（现用系统 STT，不留音频） | v0.6：端上 Whisper（NDK） |
| 顶部 5 分类色标 | ❌（只有数据层 colorTag 枚举） | v0.7 |
| 底部操作栏(删/分享/转日历/转便签/搜索) | 仅删/分享/落Obsidian | v0.7 |
| 自动网络+词典搜索 | ❌ | v0.8（可选） |
| 已完成/回收站(30天) | ❌ | v0.8 |
| 右缘把手 + 悬浮堆叠 + 浮于任意App | ✅ 已有把手+悬浮面板 | 持续打磨 |

> 结论：本项目已把"全局悬浮捕获 + 面板"骨架搭好；**最核心的差距是"录音+波形+播放+从音频转写"**，
> 这是原版的灵魂（存的是声音，不是字）。优先补这块（v0.6），再补分类色标与操作栏。

---

## 6. 美术资源 / 视觉规范（关键：零位图）
**原则：整个界面没有任何复杂图形——零位图、零插画、零重渐变。**
全部由「纯色 + 圆角形状 + 单色简单图标 + 波形线条」构成。
→ **一律用 Compose 矢量/形状绘制，不打包任何图片资源**（APK 更小、换肤更易）。

### 调色板（从视频帧采样，主体色准；分类点为标准值）
- 默认/便签 胶囊蓝：**#6695E5**（采样）
- 重要 红：**#E85450**
- 待办 橙：**#F5A623**（采样点偏亮，取饱和值）
- 待发送 绿：**#7ED87E**（采样 #8EE785）
- 灵感 紫：**#B57ED8**（采样 #B987D8）
- 强调 黄（复选框/高亮）：**#F5D76E**
- 卡片文字：白（彩色胶囊上）/ 深灰 #1C1B1F（浅色卡上）
- 背景：当前 App 的**模糊 + 暗色遮罩**（非纯黑大色块）

> **2026-08-09 联网补充（官方文字语义 vs 本表采样）**
> 官方资料对分类颜色的描述为：**黄=待办、红=重要紧急、绿=想说的话、紫=灵感**（未提蓝色）。
> 本表从演示视频逐帧采样，看到的是**蓝=便签(默认)、橙=待办、红=重要、绿=待发送、紫=灵感**。
> 差异集中在「待办」：官方文字说黄、视频采样是橙；且官方四分类无蓝色，视频有默认蓝便签。
> 本项目沿用视频采样（更接近真实界面观感）；若想完全对齐官方语义可把待办改黄，二选一即可。

### 形状与几何
- **胶囊**：圆角矩形。展开态圆角 ~18–22dp；列表/待办态更接近**全圆角 pill**（圆角 = 半高）。
- **分类色标**：纯色实心圆 + 居中**白色单色小 glyph**（📋! ✅ 💬 💡 对应 便签/重要/待办/待发送/灵感）。
- **播放键**：白色（或反色）实心圆 + 三角 ▶ / 暂停 ‖。
- **录音气泡**：圆角矩形 + **朝下的小三角尾巴**（指向 Home 键），内含实时波形。
- **波形**：对称竖条 / 折线，半透明白（彩色胶囊上）或主色（浅卡上）。当前项目已用 Canvas 画线，方向正确。
- **右缘把手**：半透明圆角竖条（左侧圆角），带一个极简笔/胶囊 glyph。

### 图标
- 底部操作栏图标全是**单色线性/填充图标**：🗑删除、📅日历、↗分享、🔍搜索、✔勾选、≡更多——
  用 Material Icons 同类替代即可，无需自绘位图。

> 落地提示：本项目已经是"纯 Compose 形状"路线，方向对。补分类色标时直接用上面的十六进制值；
> 波形/播放/气泡尾巴都用 Canvas / Shape 画，**继续保持零图片资源**。

### 图标典故（联网补充 2026-08-09）
- 闪念胶囊 App 图标 = **一红一蓝两颗胶囊（药丸）**，致敬《黑客帝国》红蓝药丸
  （"You take the blue pill… you take the red pill…"）。
- 红 ≈ RGB(233,13,13)，蓝 ≈ RGB(37,102,231)；用「渐变+圆角矩形+柔和阴影」即可画出。
- Dribbble 上有重绘思路：胶囊配**闪电**增强"快速捕捉"的速度感（可选参考，非官方）。

### 官方功能演进（联网补充 2026-08-09）
- **1.0 正式版**（2017-11，坚果Pro 2）：新增**胶囊提醒** + **胶囊附件**，并加物理快捷键。
- **3.0**（2019-10，坚果Pro 3）：智能判断「文字速记 / 网络搜索 / 语音指令」，支持多轮对话、隔空唤醒。
- 胶囊队列：右侧左滑呼出，可**拖拽调序**、单击展开、编辑、改色、删除；误删进回收站（本项目已对齐）。
- 跨应用拖拽：胶囊可拖到日历转提醒、拖到便签、拖到微信直接发（配合 One Step / Big Bang）。

### 对照本项目差距（2026-08-09 联网后）
| 官方/原版 | 本项目 |
|---|---|
| 胶囊附件（1.0） | ❌ 未做（v3 规划） |
| 提醒（1.0） | ✅ 已做（v0.16 闹钟按钮） |
| 拖拽调序 | ❌ 未做 |
| 智能判断速记/搜索/指令（3.0） | ❌ 未做 |
| 跨应用拖拽到日历/便签/微信 | 部分（有转日历按钮，无拖拽） |
